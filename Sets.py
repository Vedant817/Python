marks = {96, 98, 56, 96, 98, 99, 98}
print(marks)  # If a set contains any repetitive element then that element will be printed only ones.
# In sets there is no index. To iterate ny element, we can only  use the concepts of loops.
print(type(marks))
A = {}  # This is the syntax of the dictionary not an empty set.
a = set()  # Syntax for an empty set.
print(type(a))
